# App manifest. Platform-owned: produced by the /new-app intake, changed only via the platform team.
# The scaffolder registers this in platform/catalog/apps.yaml and the app-service Terraform module
# reads it to size the service, grant data scopes, and configure the SSO group rule.
name: meeting-vendors
title: Meeting Vendors
purpose: Paste attendee names or emails from a meeting invite; the tool looks them up against the vendor directory and shows a summary of any suppliers in the list.
owner:
  email: adam.walton+citizen@aimbition.com
  team: procurement
users:
  groups: [procurement]
data_scopes: [vendors:read]
sensitivity: internal
size: small
created: 2026-09-25
template_version: 0.1.0
